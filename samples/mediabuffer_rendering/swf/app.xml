<?xml version="1.0" encoding="utf-8" ?>
<application xmlns="http://ns.adobe.com/air/application/51.0">

    <id>com.harman.air.MediaBufferRendering</id>

    <!-- Used as the filename for the application. Required. -->
    <filename>MediaBufferRendering</filename>

    <!-- The name that is displayed in the AIR application installer. May have multiple values for each language. See samples or xsd schema file. Optional. -->
    <name>Test MediaBuffer Rendering</name>

    <!-- A string value of the format <0-999>.<0-999>.<0-999> that represents application version which can be used to check for application upgrade.
         Values can also be 1-part or 2-part. It is not necessary to have a 3-part value.
         An updated version of application must have a versionNumber value higher than the previous version. Required for namespace >= 2.5.
         For iOS Apps, this represents build number. -->
    <versionNumber>0.0.1</versionNumber>

    <!-- Identifies the ActionScript extensions used by an application. -->
    <extensions>
        <extensionID>com.harman.ane.MediaBufferRender</extensionID>
    </extensions>
	<supportedProfiles>extendedDesktop</supportedProfiles>

    <!-- Settings for the application's initial window. Required. -->
    <initialWindow>
        <content>MediaBufferRender.swf</content>
        <visible>true</visible>
        <fullScreen>true</fullScreen>
    </initialWindow>

</application>
